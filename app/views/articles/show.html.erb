<div>
  <%= @article.title %>
</div>

<div>
<%= @article.content %>
</div>

<%= form_for([@article, ]) do |f| %>
   <div>
     <%= f.label :message %><br>
     <%= f.text_area :message %>
   </div>

   <div>
     <%= f.label :email %><br>
     <%= f.email_field :email  %>
   </div>

   <div>
       <%= f.submit "Submit Comment" %>
   </div>
<% end %>
